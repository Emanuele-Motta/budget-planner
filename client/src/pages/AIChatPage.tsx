import { Card, CardContent, Stack, Typography } from '@mui/material';

export const AIChatPage = () => (
  <Stack gap={2}>
    <Typography variant='h4'>AIChat</Typography>
    <Card><CardContent><Typography>Sezione AIChat pronta per estensioni (CRUD, notifiche, export/import, multi valuta).</Typography></CardContent></Card>
  </Stack>
);
