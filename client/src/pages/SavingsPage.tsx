import { Card, CardContent, Stack, Typography } from '@mui/material';

export const SavingsPage = () => (
  <Stack gap={2}>
    <Typography variant='h4'>Savings</Typography>
    <Card><CardContent><Typography>Sezione Savings pronta per estensioni (CRUD, notifiche, export/import, multi valuta).</Typography></CardContent></Card>
  </Stack>
);
