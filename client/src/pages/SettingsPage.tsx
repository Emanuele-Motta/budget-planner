import { Card, CardContent, Stack, Typography } from '@mui/material';

export const SettingsPage = () => (
  <Stack gap={2}>
    <Typography variant='h4'>Settings</Typography>
    <Card><CardContent><Typography>Sezione Settings pronta per estensioni (CRUD, notifiche, export/import, multi valuta).</Typography></CardContent></Card>
  </Stack>
);
