import { Card, CardContent, Stack, Typography } from '@mui/material';

export const AIAnalysisPage = () => (
  <Stack gap={2}>
    <Typography variant='h4'>AIAnalysis</Typography>
    <Card><CardContent><Typography>Sezione AIAnalysis pronta per estensioni (CRUD, notifiche, export/import, multi valuta).</Typography></CardContent></Card>
  </Stack>
);
