import { Card, CardContent, Stack, Typography } from '@mui/material';

export const AccountsPage = () => (
  <Stack gap={2}>
    <Typography variant='h4'>Accounts</Typography>
    <Card><CardContent><Typography>Sezione Accounts pronta per estensioni (CRUD, notifiche, export/import, multi valuta).</Typography></CardContent></Card>
  </Stack>
);
